{
  "tasks": {
    "check": "deno fmt --check && deno lint && deno test",
    "lint": "deno lint",
    "format": "deno fmt",
    "test": "deno test --allow-all",
    "audit": "deno task audit"
  },
  "compilerOptions": {
    "strict": true,
    "noImplicitAny": true
  }
}
