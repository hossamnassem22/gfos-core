# دليل الصيانة المعيارية
1. **تحديث النظام:** لا يتم إلا عبر `pipeline.yaml` بعد اجتياز الاختبارات.
2. **التعامل مع الانتهاكات:** يُمنع التدخل اليدوي؛ النظام يتصرف ذاتياً.
3. **التدقيق:** البصمات الرقمية موجودة في `AuditEntry`.
